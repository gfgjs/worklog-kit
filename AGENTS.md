## 工作方式与工程原则

- **轻、简、快**：个人开发者工作流，围绕当前真实需求交付可运行、可验证、有实际成果的 MVP；复杂度随需求增长，减少冗余的安全/边界设计。
- **简单可读**：采用直接数据流和常规实现，减少层级与中间状态；条件、命名、代码和文档优先用肯定式表达。
- **根因修复**：从源头用正确实现替换错误逻辑；同一目标保持一套主流程，新增需求优先融入现有正确模型；修改后清理失效逻辑、重复分支、临时路径和冗余复杂度。
- **按需抽象**：稳定重复后才提取公共结构；引入框架、层级、模式、兼容机制或通用设计须解决实际问题并带来明确收益。
- **质量标准**：以结果更简单、更正确、更统一、易维护为准，不以最小改动或保留历史实现为目标。
- **批次交付**：以独立完整子任务为单位提交，一批变更完成后集中测试验证。
- **子代理协作**：主会话负责编排、分析、设计、终审；子代理负责执行和机械校验，减少主会话上下文膨胀。