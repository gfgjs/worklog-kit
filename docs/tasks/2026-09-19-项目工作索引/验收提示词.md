# 完整验收提示词

将下面提示词复制到新的会话中执行：

---

请对 `C:\workspace\worklog-kit-public` 的「项目工作索引与按需上下文」改动做完整独立验收。本次是验收，不重新施工；不使用子代理，不提交或发布。允许创建临时测试副本与验收记录，不改产品代码、不覆盖已有未提交改动，发现问题先报告。

先读仓库 AGENTS.md，再从 `docs/tasks/2026-09-19-项目工作索引/state.md` 接续，读取 details 的目标与验收、当前设计、执行结果，沿结果指针补读相关 T 单元设计、共同约束和约束片段。以 `docs/designs/2026-09-19-项目工作索引与按需上下文详细方案.md` 的第 12、16 节为主要验收依据，其余章节按需核实；该方案已授权施工，文中旧提案时点的未授权措辞不覆盖本次授权。不要把 CLI accept 默认输出当完整读集，不一次加载全部历史。

检查当前工作区（包含已跟踪差异和新文件），不要只看 HEAD。原施工开始前已有 docs/README.md 改动以及工作索引方案和审查报告两个未跟踪文件，审查报告不是本轮实现证据。执行者只跑简单验证，不据「已自检」认定通过。

请按以下范围验收，并记录实际命令、退出码、观察结果与证据：

1. 索引与文档：README/todo 只导航，todo 三组与空组「无」正确；候选从主题可发现；已指定任务不自动加载索引或全部主题；本项目全局说明为 docs/topics/项目推进.md。核对根 README、usage、Skill、模板、设计替代指针的一致性。
2. CLI：无参 todo 存在可读时来源标识加完整原文及 list 提示，无链接展开、README 注入、语义摘要或截断；候选而无任务也正常。缺失回退为 0，空/空白为 1，目录与真实读取失败为 2。设计可稳定复现的读取失败探针；环境无法模拟时明确标未验证。--list 不依赖 todo，枚举全部未完成任务并提示缺少 state，排除完成/已取消但仍允许显式查看。覆盖 --list 与任务/role/unit 的互斥、无任务携带 role/unit、帮助版本在不同位置的优先级与主题不能作任务目标。
3. 角色材料：无角色只输出 state 三节，不含额外章节与必读展开；四角色读集保持原契约。临时任务含共享必读、T1/T2 各自专属片段和片段中的二层引用，确认只展开一层，implement T1 带共享/T1、不带 T2；普通引用不展开。公共必读对每个单元生效。
4. 验收补读：在临时任务中让 T1 有公共概要未复述的专属约束，执行结果仅给依据指针并故意包含不符合约束的结果。按 accept 输出沿指针补读 T1/适用共同约束及片段，应能识别违约且不加载无关 T2。用实际读集与判断证明，不能只检查文档有没有写这句话。
5. 事项流转：在临时副本分别演练未立项、立项、全部解决、明确放弃、部分承接、暂停、取消。全部解决和放弃须撤下 todo 事项（分别覆盖指主题锚点与任务 state），成果进现状，证据引用修复，无关事项及有导航价值的主题入口保留；空组写无。A/B 部分承接中 A 完成不能删除 B 的剩余范围、启动条件和指针；暂停取消保持可回找；不把验收范围内未完成内容移回主题制造完成。
6. 全局视图：放入一个未被 todo/主题登记的未完成任务，使用全局说明、相关主题及 --list、按需 state 聚合判断，不能只据 todo 宣称全局完成。无 CLI 用任务目录做等价定位。
7. 检查和导出：todo/主题坏链接和坏锚点应被 check 报出，修复与回收后通过；无 todo/无主题目录项目仍合法。init 自然导出 todo/topic 模板，不创建消费项目的 todo，不改 AGENTS，原冲突预检契约仍成立。
8. 集成：运行完整 npm test、check docs 与必要的示例文档检查；npm pack 后在临时消费项目验证 init、无参 context、--list、四角色 context 与 check。单独复制 Skill，在不依赖 CLI/包内源码路径的条件下演练代表性任务的选择与交接。title-search 原有 T2 是故意待施工的红测试，不把原样例红测试误判为产品回归；演练施工只在临时副本完成。

保持轻量，复用一个代表性任务与临时夹具覆盖场景，不做无关全仓重构或历史迁移。报告给出逐场景「通过/失败/未验证」、可复现问题的期望与实际、定位、严重性及风险；结构检查通过不能代替语义验收。

已知检查边界：直接 `check skills/worklog` 会把模板 state 的待填写阶段当作真实任务而报错，施工记录已核对该占位符在 HEAD 中原本存在。请区分模板与填好后的消费项目任务，不为通过检查而给模板伪填真实阶段。

将报告放在本任务目录，并把证据写入 details。全部验收条件确实通过才将本任务改为完成，并按治理规则更新项目推进、回收对应 todo 条目和修复引用；不关闭其它历史待验收任务。有失败则回到相应施工单元并写清阻塞；环境导致关键场景未验证时保留待验收。不要未经授权修复产品代码。最后给出是否可验收的明确结论与必要后续动作。
