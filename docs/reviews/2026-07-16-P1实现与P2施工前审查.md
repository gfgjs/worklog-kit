---
id: 2026-07-16-P1实现与P2施工前审查
status: snapshot
type: review
line: worklog-kit-oss
created: 2026-07-16
---

# 第四轮审查 · P1 实现对照 + P2 施工前把关(2026-07-16)

> ✅ **六项已裁(用户,2026-07-16 当日),回写完成**——裁 1:中文文件名一等公民(用户第一需求;NFC 门兜底,失灵再修,方案授权本轮定案);裁 2~6 全采纳建议。裁决记录与回写去向以**方案 §19 为准**;本文自此为时点快照(status: snapshot),§2/§3 表格保留裁决前原貌。R4-01/02 修码已批**待施工**;P2 未开工。

> **定位**:v0.3 施工基线(三轮 28 项已裁)之后的**首轮带码审查**——前三轮审的是方案文本,本轮审两件事:①已施工的 P1 代码是否兑现方案;②P2 开工前还缺哪些契约。
> **范围**:`src/` `bin/` `skills/` `templates/` `schema/` `locales/` 全量精读 + [设计方案 v0.3](../designs/2026-07-12-工作记忆治理体系开源工具化方案.md) + P2 三件套。不回炉 L1–L12 已锁基线。
> **方法**:逐文件精读 + 门禁实跑取证 + 可疑行为最小复现(非静态目测)。
> **状态**:全部发现 **pending 待裁**。按「规范文本回写红线」,方案正文契约未动,仅在方案 §19 登记索引(与 §16/§17 待裁期同款处理)。

## §1 实证基线(2026-07-16,HEAD = `6f6b212`)

| 项 | 结果 |
|---|---|
| `npm run selftest` | ✅ 全绿(check 24 例 + index 6 例 + skills 7 例 = 37) |
| `worklog check`(本仓) | ✅ exit 0(11 文档,0 代码文件——`sourceRoots` 置空系 dogfood 特例) |
| `worklog index`(本仓) | ✅ exit 0 |
| `worklog doctor` | exit 0;报 Codex 侧 SKILL.md 漂移(已裁暂缓,todo 在账) |
| `parseJsonc('{"a": "x,}" }')` | ❌ 返回 `"x}"`——尾逗号正则误伤字符串值(R4-04 实证) |
| `grep todo.md src/init.mjs` | ❌ 零命中——init 不产滚动状态靶点(R4-01 实证) |
| 本仓 `line:` 值盘点 | 4 个 distinct:`worklog-kit-oss` / `文档治理` / `建仓与v0.3基线` / `P2生成式索引与契约收敛`;**3 个纯中文**(R4-15 的 dogfood 撞点) |

**总评**:P1 实现质量高于典型 MVP——三门自检齐、closeout 契约按 §7.1 全量落地、fixture 自扫描免疫等 Scrollery 教训都接住了;门禁判定方向(正负例)未发现错判。发现集中三类:①P1 的**消费者路径**缺口(dogfood 遮蔽:「包=仓」时测不出 thin-runner 的路径断裂);②已裁决项(D-002 等)的**残留清剿**;③P2 开工前**未闭合的契约**——最重一条(R4-15)是方案内两条已裁原则互斥,阶段 2 第一天就要答题。

## §2 发现清单

严重度沿用房规:🔴 阻塞施工 / 🟠 施工前应补齐 / 🟡 记账随文清理。

### A. 代码实缺陷(P1 已施工面;修码须按卷首红线另行批准)

| ID | 严重 | 发现 | 证据 / 影响 | 建议修法 |
|---|---|---|---|---|
| R4-01 | 🟠 | **init 不 stamp `docs/todo.md`,而模板配置声明 `dispositions[todo].target=docs/todo.md`(targetKind=fixed,门禁验存)** | `src/init.mjs` buildItems 无 todo 项。F-003 已定性此为「真死配置」并在本仓手工修复(2026-07-16 建 todo.md),但**修复未回流 init**——每个新 init 仓都复刻同一死配置,首个 todo 候选必撞 `closeout.docsMissing`;skill 收口第 7 步与收口手册 §三.7 的靶点在新仓同样落空 | init 增 stamp todo.md 模板(空表骨架) |
| R4-02 | 🟠 | **SKILL.md 与收口流程引用包内 `templates/` 路径,消费仓不可达** | SKILL 起步第 3 步「按 `templates/` 三件套模板写入」、收口第 3 步「模板见 `templates/closeout-template.md`」——thin-runner 拓扑下 templates/ 驻 npm 包,init 后的消费仓根目录**没有**该目录(runbook 被 stamp 到 `docs/runbooks/closeout.md`,trio 模板完全无仓内落点)。dogfood 仓恰好「包=仓」故全绿,**本仓测不出**;Scrollery 装入(P2 阶段 0 靶场)第一次建三件套即撞 | skill 文本改指 stamped 落点;trio 模板要么 init 时 stamp(如 `docs/planning/_templates/`),要么 skill 内嵌骨架。与 R3-3「skill 收口靶点参数化」同批做 |
| R4-03 | 🟡 | R3-6(frontmatter 单一解析)在 P1 自身未贯彻 | `lib/frontmatter.mjs` 有 `parseFrontmatter`,但 `check-docs.mjs` L171–185 自带一套 BOM/`---`/regex 解析——双引擎已在仓内,R3-6 预言的漂移条件已成立 | check-docs 改用 parseFrontmatter,单一实现 |
| R4-04 | 🟡 | `parseJsonc` 尾逗号正则误伤字符串值 | 实测 `{"a": "x,}" }` → `"x}"`:`,(\s*[}\]])` 剥注释后作用于含字符串的全文。现有配置键含 `,}` 概率低,但坏法是**静默改值**,门禁按错配置跑 | 尾逗号剥离并入 stripJsonc 状态机(字符串内不替换);或 ajv 阶段一并换正规 JSONC 解析 |
| R4-05 | 🟡 | 展示面文案未全走 catalog | 三个 selftest 的全部输出、check-docs main 的 mode 字串(「仅断链」等)硬编码中文;locales 的 `selftest.pass/fail` 是**死键**。P1 判据「文案走 catalog」只对门禁报错成立 | selftest 输出接 t();或明示 selftest 属开发者面豁免 catalog(写进方案 §5),二择一 |
| R4-06 | 🟡 | 文案与行为不符:`docs.missingFrontmatter` 称需 `status/type/line/created`,门禁实际不校验 `line` | P1 `line` 零校验是设计(F-001 语境),但报错文案宣称必填 | 文案去 line;或 P2 阶段 2 上引用门时一并收紧 |
| R4-07 | 🟡 | stale-trio 检测用文件 mtime | `doctor.mjs` L40;fresh clone 后所有 mtime=克隆时刻,烂尾检测永不触发——恰在多人/换机场景(第三支柱)最需要它时失效 | 改 `git log -1 --format=%ct -- progress.md`;git 不可用再回退 mtime |
| R4-08 | 🟡 | 配置未知键静默忽略 | schema 声明 `additionalProperties:false`,运行期 validateShape 不查未知键——键名打错(如漏字母的 `deprecatedStatus`)静默使门禁变松 | validateShape 加未知键警告 |
| R4-09 | 🟡 | e2e 未脚本化 | 上一任务 closeout 称「e2e 过」,系一次性手工验证;仓内无 `test/`(§6 布局承诺的目录不存在),回归靠手 | 最小 e2e 脚本(temp 仓 init → 双门绿 → 制造违规 → 变红),挂 `npm run e2e` |
| R4-10 | 🟡 | init 忽略已有 `.worklogrc.jsonc` | bin 不给 init 传 loadConfig 结果,init 硬用 DEFAULTS——已改 docsDir 的仓重跑 init 按 `docs/` stamp(幂等跳过兜一部分,新文件仍落错位) | init 存在配置时改用 loadConfig |
| R4-11 | 🟡 | 仓根文件是扫描盲区 | 1a 只 walk docsDir,1b 只扫 sourceRoots **目录**——仓根 README.md 等根文件两边都不在:根 README 指向 docs 的链接断了无人报 | 仓根文件并入 1b 扫描;或文档明示盲区 |

### B. 文档一致性(已裁决项的残留清剿)

| ID | 严重 | 发现 | 证据 / 影响 | 建议修法 |
|---|---|---|---|---|
| R4-12 | 🟠 | **D-002(四档砍两档)未贯穿全文** | 残留三处:方案 §14 glossary `profile` 词条仍列四档;仓根 README「设计取舍」仍列四档;`schema/worklogrc.schema.json` profile enum 仍四值(阶段 0 还须为 brownfield 扩 baseline 键)。与 R2-C1「裁决未贯穿」同病型 | 一次清剿;schema enum 收敛 `strict|brownfield`,`--warn-only` 归 CLI 标志 |
| R4-13 | 🟡 | 滚动状态与配置的小账三笔 | ① `docs/todo.md` 工作线表原漏 `worklog-kit-oss`(设计文档自身的 line 值;P2 引用门要 4 个实体非 3)——**本次审查已补行**;② `.worklogrc.jsonc` dirs 含 `archive` 而本仓无此目录(config.dirs 仅 init 用、无门校验,但账应平);③ docs/README 维护规则「本仓当前无 reviews/」随本报告建 reviews/ 已过时——**本次已改** | ②随文清理或明示容忍 |
| R4-14 | 🟡 | P2 task_plan「本仓 8 个现役文档」口径不明 | `worklog check` 实扫 11 个 .md:6 个三件套豁免件 + 5 个受 frontmatter 校验件(README / todo / 设计方案 / worklogs README / closeout)。「8」对不上任何一种口径 | 阶段 1 迁移清单以 `worklog check` 实扫为准,勿按 8 估工 |

### C. P2 施工前契约缺口(写码前须裁)

| ID | 严重 | 发现 | 证据 / 影响 | 建议裁决 |
|---|---|---|---|---|
| R4-15 | 🔴 | **线实体 slug 字符集未裁,且两条已裁原则在此互斥** | R3-1 把「文件名约定」明文划入机器面锁 ASCII;F-001 结论「slug 可自动派生、人工只复核不发明」的隐含前提却是**允许中文文件名**(Scrollery 的 line 值多为中文;本仓 4 个 line 值 3 个纯中文)。若 `lines/<slug>.md` 锁 ASCII:中文→ASCII 须音译/翻译 = 人工发明,F-001「零人工重命名」不成立;若允许中文文件名:macOS(NFD)与 Windows/Linux(NFC)的 Unicode 归一差异可让「同名」实体在异平台成**不同文件**——Q7 卖点(撞名 = add/add 冲突原生暴露)恰在跨平台撞名时**失灵**,§7.2 M-10「纯 ASCII 排序」承诺同被打破。**阶段 2 第一天就要答题;本仓 dogfood 自身即撞点** | 三选一(详见 §3 裁 1):①slug 锁 ASCII + 迁移命令对中文值出「待人工命名」清单;②允许中文文件名 + 门禁强制 NFC 归一;③`line` 保持人可读中文、实体文件名用确定性派生(如拼音/短 hash)+ frontmatter 携原名 |
| R4-16 | 🟠 | **brownfield baseline 承载未设计** | 阶段 0(已前移为 P2 第一阶段)要求「存量违规入 baseline 豁免清单,仅新增违规 enforce」,但:清单存哪(独立文件?配置内嵌?)、违规钥匙怎么编(路径+规则名?内容 hash?文件改名后豁免是否失配?)、如何再生成(`worklog baseline --update`?)、baseline 文件自身是否受治理——全部未定。P2 第一个开工阶段无此设计寸步难行 | 定案后写入方案 §7.5(建议见 §3 裁 2) |
| R4-17 | 🟠 | **分档后的 `index` CLI 语义未钉死** | 现役 `worklog index` = 不变量门;§4.1 item4 裁的生成式档 CLI = `index build` + `check`。P2 后同一命令跨档语义分叉,而 npm script、CI 模板、README 已把裸 `worklog index` 固化为门禁——不先钉死,R2-C1 式「两套 check」将在 CLI 层重演 | 建议:显式子命令 `index build` / `index check`;裸 `index` = 按档别名并打印所指(见 §3 裁 3) |
| R4-18 | 🟠 | **D-004 no-leak 与仓内现状冲突:Scrollery 值样例已入仓已提交** | D-004 裁「Scrollery 内容一律不入仓」;但方案 §4.1 item2 ④ 正文与 P2 findings/progress 已引用真实 line 值样例(6 处,此处不复列)与登记表行注,正是 §8.1 点名「54 个 line 语义值」类必清私料,且已在 git 历史(`6f6b212` 及更早)。拖到 P5 转公开再清 = 重写全部历史或 fresh-export 断档,成本最高 | 三选一(见 §3 裁 4):①修订 D-004 边界给「文档内少量值样例」豁免(转公开前替换为合成值即可,不追历史);②趁未发布现在重写本地历史清除;③P5 fresh-export 断历史 |
| R4-19 | 🟡 | L12/D-005 元判据未在 P2 task_plan 落地 | 规则要求「每条完成判据自带可测性标注」,task_plan 七阶段判据仅阶段 7 拆了可测性,其余六阶段无标注——该规则的诞生原因就是同病已复发两次 | 开工前给每阶段判据补「可测:是/否 + 方式」 |

## §3 待裁清单(汇总;裁后按红线回写方案正文)

| # | 待裁事项 | 选项(建议加粗) | 解锁 |
|---|---|---|---|
| 裁 1 | **R4-15 线实体 slug 字符集** | ① **slug 锁 ASCII,迁移命令对中文值出「待人工命名」清单**(与 R3-1 一致,代价:F-001「零人工」降级为「半自动」);② 允许中文文件名 + NFC 归一门;③ 中文 line + 派生 ASCII 文件名 + frontmatter 携原名 | P2 阶段 1/2 |
| 裁 2 | **R4-16 baseline 承载** | ① **独立 `.worklog-baseline.json`,钥匙 = 相对路径 + 规则 key(+可选内容 hash),`worklog baseline --update` 再生成,文件自身入库受治理**;② 配置内嵌 `.worklogrc.jsonc`;③ 内容 hash 为主键 | P2 阶段 0 |
| 裁 3 | **R4-17 index CLI 语义** | ① **`index build` / `index check` 显式子命令;裸 `index` 按档别名并打印所指**;② 裸 `index` 直接报错要求子命令(破现役 CI/脚本);③ 生成式档另起动词(如 `worklog build`) | P2 阶段 4 |
| 裁 4 | **R4-18 no-leak 样例处置** | ① **修订 D-004:计数/形态可入仓,值样例限額并于 P5 前替换为合成值(不追历史,转公开走 fresh-export 时一并断)**;② 现在重写本地历史清除;③ 全部推迟 P5 | P5 前任意时点,越早越便宜 |
| 裁 5 | **A 类修码批准**(卷首红线:写码须另行批准) | ① **批 R4-01/02(🟠 两项,消费者路径断裂)先修,🟡 九项并入 P2 顺手修**;② 全部并入 P2;③ 全部立即修 | P2 阶段 0 之前(Scrollery 靶场装入前 R4-01/02 必须在) |
| 裁 6 | **B 类残留清剿批准**(执行已裁决项,无新决策) | ① **批准即清**(R4-12 三处 + R4-13② + R4-14 口径更正 + R4-19 标注);② 并入 P2 各阶段 | 随时 |

## §4 与既有轮次的关系

- 不回炉 L1–L12;本轮 0 项推翻前三轮结论。
- A 类多数是「方案对、实现漏」的执行债;B 类是 D-002 等已裁项的贯彻债(R2-C1 同病型第二例);C 类是前三轮审文本时**审不出来**的缺口——只有对着真代码和真数据(本仓 line 值、Scrollery 形态)才暴露。
- 方案 §19 为本报告的登记索引;裁决后按「规范文本回写红线」改写正文,本报告届时翻 `status: snapshot` 留痕。
