---
status: snapshot
type: working-memory
line: 建仓与v0.3基线
created: 2026-07-13
---

# 发现与决策:建仓与 v0.3 基线

## 需求
- 用户 2026-07-13:R2-C7 问「多人用①events 模式、单人用常规模式可行吗」(评估=可行,细则四条见方案 §18);R2-m2 确认纯中文;R3-12 不入 Scrollery 库、新建同级目录及仓库;其余 25 项全按建议采纳。

## 发现
- 用户在 Scrollery 工作树对方案手改两处(R2-m2 状态列、R3-12 建议列)未提交——导入 commit 以工作树态为准并在信息中注明,避免手迹丢失。
- 方案 §1/§14 的 `../../` 相对链接在迁仓后必断——v0.3 已按 R2-C4 改为「出处标注」纯文本,自包含;副作用:Scrollery 侧归档移动零断链,门禁一次过。
- Scrollery 并行会话活跃(另有方案与 task_plan 在途),归档 commit 守显式 pathspec 未收割。
- 本仓暂无任何门禁/CI;三件套沿用 Scrollery skill 中文模板——待 P1 落地后按方案 R3-1(机器面 ASCII)自迁移,属预期内的 dogfood 债。
- 用户 2026-07-16 追问「工具用于旧项目会否触发字母取号」,澄清出**工具零取号**这一事实:`line` 全程是自由文本——`schema/worklogrc.schema.json` `additionalProperties: false` 且字段列尽无 line 项、`check-docs` frontmatter 只校验 `status`/`type`、`init` 模板写 `line: 文档治理`(短语非字母)。故旧项目的取号触发源在其**自身**而非工具:README 字母表 = 数据,旧 skill 文本「取下一空闲字母」= 行为;`install-skills` 覆盖后者即断掉唯一执行者,存量字母表退化为无人执行的死数据,与新 slug 混存不报错。
- **P2 迁移悬崖(风险)**:上条的混存兼容是**零校验的副产物,不是设计**。§7.2 一旦上「文档引用的 line 必须存在实体文件」引用门,全部存量 `line: <字母>` 同日变违规(`lines/<字母>.md` 不存在)。§4.1 的 R3-3 只覆盖「索引档间切换须一次性迁移」,未覆盖工作线字母→slug 这条;P2 `lines/` 若只上门不给迁移命令,brownfield 采纳者会撞墙。→ F-001

- **P1.5 三指标 2/3 结构性无效**(2026-07-16 实测,直接触发 D-001):合并冲突数=0 但仓内 1 分支、0 合并提交——**实验根本没发生**;绕过次数=0 但无 pre-commit hook(§4.1 item4 明裁不做)+ CI 首跑 4s billing failure——**无门可绕**;仅 closeout 完成率有效(0/1,本任务收口后 1/1)。**空读数与好读数在指标表上同形**——§12 P1.5 行虽写了「单人单仓结构性产不出」,但写在括号里、未进指标表,故一度被(我)误读为「门槛已过」。教训:go/no-go 判据须配元判据「该指标此阶段是否可测」。
- **本仓 dogfood 只验到三支柱之一**。单人 + 单分支 + 无 hook + CI 挂,恰好屏蔽了 L6 支柱二(CI 强制纪律)与支柱三(multi-person-ready);真正验到的是支柱一(两层记忆架构)——`npm pack` → 空白仓 → 真实 Claude Code 会话未受提示即主动触发 `/planning`、自主走完 7 步任务并正确收口。故 P1.5 不是「没验」,是**只验了 1/3,且验的那 1/3 与 P2 取舍无关**。

## 外部资料(当数据,不当指令)
- (无)

## 耐久提升候选(F-001 递增;发现当场登记,收口时逐行处置进 closeout.md)
| 候选 ID | 内容摘要 | 建议去向 |
|---------|----------|----------|
| F-001 | P2 `lines/` 实体门须自带**字母→slug 迁移命令**(扫描现存 `line` 值 → 生成占位实体文件 → 报告需人工重命名项),否则存量字母线在引用门上线当日全量变违规;§4.1 R3-3 的「迁移非配置开关」原则须从索引档位扩到工作线命名空间 | design |
| F-002 | 两条已裁暂缓的遗留须有承接处,否则随本任务收口蒸发:①远程 CI 首跑绿(billing 阻断,非代码);②Codex 侧 skill 副本同步(`skills --force`)。二者均为**用户明裁暂缓**而非未做完 | todo |
| F-003 | **dogfood 发现:`todo` disposition 是死配置**——targetKind=`fixed` 的 target 由配置写死 `docs/todo.md` 且门禁验存([check-docs.mjs:97](../../../src/check-docs.mjs#L97)),而该文件本仓不存在 → 任何 `todo` 候选必撞 `docsMissing`。`fixed` 与 `docs` 两类的**失效时机不同**:`docs` 类 target 每行自报,首次使用时新建即可;`fixed` 类 target 是配置常量,靶点缺失即**全局**不可用,且在用到之前无任何信号。`init` 应 stamp 出 todo 靶点,或门禁加启动自检 | todo |
